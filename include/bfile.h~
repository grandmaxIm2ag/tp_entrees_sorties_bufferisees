#ifndef __BIFLE_H_
#define __BFILE_H_

struct bfile;
typedef struct bfile bfile;

bfile * bOpen (char * path, char mode);
int bclose(bfile * bf);
int bWrite(void * p, int size, int nb_element, bfile * bf);
int bReader(void * p, int size, int nb_element, bfile * bf);

#endif
